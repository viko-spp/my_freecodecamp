//=============   решение с помощью метода substr();

function confirmEnding(str, target) {
  
  return str.substr(str.length - target.length, target.length) === target;
}

confirmEnding("He has to give me a new name", "name");

//===========   решение с помощью метода substring();

function confirmEnding(str, target) {
  
  return str.substring(str.length - target.length) === target;
}

confirmEnding("He has to give me a new name", "name");





для понимания работы данных методов 

substring() - https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/String/substring

substr() - https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/String/substr
