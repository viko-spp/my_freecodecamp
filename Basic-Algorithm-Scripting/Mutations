function mutation(arr) {
  var oneWord = arr[0].toLowerCase();
  var twoWord = arr[1].toLowerCase();
  
  for(var i = 0; i < twoWord.length; i++){
    if(oneWord.indexOf(twoWord[i]) < 0){
      return false;
    }
  }
  return true;
}

mutation(["hello", "hey"]);
