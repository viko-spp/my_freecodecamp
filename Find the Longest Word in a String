//#1

function findLongestWord(str) {
  var arrStr = str.split(' ');
  var maxStr = 0;
  for(var i = 0; i < arrStr.length; i++){
    if(maxStr < arrStr[i].length){
      maxStr = arrStr[i].length;
    }
  }
  return maxStr;
}

findLongestWord("The quick brown fox jumped over the lazy dog");
